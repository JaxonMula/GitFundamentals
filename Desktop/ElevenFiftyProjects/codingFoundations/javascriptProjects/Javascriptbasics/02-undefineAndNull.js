//Undefined -. a value that hasn't been interacted with yet/

let coffeeCupContents;

console.log("coffeeCupContents:",coffeeCupContents); //undefined in the console

//null -> when we want to make a variable = "nothing", we'll use null
let userData = "User data goes here...";
userData = null;

console.log("userData: ",userData);